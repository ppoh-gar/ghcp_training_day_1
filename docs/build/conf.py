"""Sphinx configuration auto-generated by build_sphinx_docs.py."""
import sys

# Source dir itself (for bare-name imports) and its parent (needed when the
# source dir has an __init__.py, so apidoc names modules after the dir itself,
# e.g. `Ticket Desk.<module>`, which is only importable with the parent on path).
sys.path.insert(0, r"C:\temp\ghcp_training_day_1")
sys.path.insert(0, r"C:\temp")

project = "Ticket Desk"
extensions = ["sphinx.ext.autodoc", "sphinx.ext.napoleon", "sphinx.ext.autosummary"]
napoleon_google_docstring = True
napoleon_numpy_docstring = False
autosummary_generate = True
exclude_patterns = ["_html", "_build"]
html_theme = "alabaster"
